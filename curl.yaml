apiVersion: v1
kind: Pod
metadata:
  labels:
    run: curl
  name: curl
spec:
  serviceAccountName: jenkins
  automountServiceAccountToken: false
  containers:
    - image: curlimages/curl:latest
      name: curl
      command: ["/bin/sh", "-c"]
      args:
        - |
          while true; do 
            curl -I http://nginx-deploy.default.svc.cluster.local:80;
            sleep 1;
          done
      resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
